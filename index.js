/**
 * Fraud Detection Mobile Tests - Entry Point
 */
console.log('Fraud Detection Mobile Tests');
console.log('Run "npm start" to start both the mock server and tests');
console.log('Run "npm run start-server" to only start the mock API server');
console.log('Run "npm test" to only run the tests');
console.log('Run "npm run report" to generate the Allure test report');
